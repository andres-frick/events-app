version: "3"
services:
  backend:
    build:
      context: ./backend
      dockerfile: dockerfile
    ports:
      - 4000:4000
  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - 80:80